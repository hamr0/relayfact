// stub.mjs — the no-op the validity gate must reject as VACUOUS: "every version is equal precedence". Any
// suite green against this asserts nothing about ordering.
export default function compareSemver() {
	return 0;
}
