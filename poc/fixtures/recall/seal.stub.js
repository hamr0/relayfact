// Stub — run 2's task (probe-07 apply phase). seal() needs the SAME project signing salt as sign().
// The worker has no read tool and is given NO runbook this run: the salt is reachable only via a recall
// of the lesson run 1 stored from its own passing code. See probe-07.
export function seal() {
  return 'TODO';
}
