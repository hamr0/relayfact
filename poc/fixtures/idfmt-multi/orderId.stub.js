// Run target. Apply the project entity-ID convention to a NEW entity. The literal answers are NOT
// in any recalled note; the rule must be applied (prefix ORDER, its check letter, width-6 pad).
export function orderId(n) {
  return 'TODO';
}
