// Run target. Apply the project entity-ID convention to a NEW entity. The literal answers are NOT
// in any recalled note; the rule must be applied (prefix TICKET, its check letter, width-6 pad).
export function ticketId(n) {
  return 'TODO';
}
