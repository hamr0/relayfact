// Run 2 (apply). DIFFERENT entity (order, not user) — needs the SAME rule applied to a NEW prefix and a
// NEW check letter. The literal answers are NOT in the run-1 lesson; the agent must adapt the rule.
export function orderId(n) {
  return 'TODO';
}
