// Run 1 (learn). Implement per the project runbook (readable this phase). The convention is NOT obvious
// from the function name alone — width-6 padding and the check-letter rule must come from the runbook.
export function userId(n) {
  return 'TODO';
}
